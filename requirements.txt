# HTTP / scraping
requests>=2.31.0
aiohttp>=3.9.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Browser engine: Camoufox — a debloated Firefox fork with C++-level
# fingerprint spoofing and its own built-in Playwright driver
# (camoufox.async_api). No Chrome, no truedriver. The Camoufox browser
# build itself is fetched with `python -m camoufox fetch` (Dockerfile).
camoufox>=0.5.0

# Web app (dashboard)
flask>=3.0.0

# Email checker (MX record lookups; optional - falls back to plain DNS)
dnspython>=2.6.0

# Offline captcha solver models (optional; server falls back to the vision
# model when these are absent). Training/tools: make_dataset.py,
# make_challenges.py, train_models.py, tile_classifier.py.
Pillow>=10.0.0
numpy>=1.26.0
# torch (CPU build is plenty): pip install torch --index-url \
#     https://download.pytorch.org/whl/cpu
