# All vaultproxies sessions were dead (0/530 Discord-reachable), so the
# list is empty and the workers run over TOR.
#
# To go back to residential proxies: paste fresh sessions here, one per
# line, as user:pass@host:port — the app hot-reloads this file. Also make
# sure FORCE_TOR is unset, or it will keep ignoring them.
